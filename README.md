# Subdomain Finder Web Application

A modern, professional web interface for orchestrating subdomain discovery workflows. The UI is built with React, Tailwind CSS, shadcn-inspired components, and rich dashboards for scan progress, results, and history.

## Features

- Guided scan configuration with validation and advanced options
- Live scan progress monitoring with animated stats
- Results table with export actions and filtering controls
- Dashboard visualizations powered by Recharts
- Wordlist manager with upload and editor panels
- Scan history table with quick actions
- Theme toggling and configurable API settings

## Tech Stack

- React 18 + TypeScript
- Vite build tooling
- Tailwind CSS styling
- TanStack Query, Axios, Zod, React Hook Form
- Recharts data visualization
- Zustand state management

## Getting Started

```bash
npm install
npm run dev
```

## Scripts

- `npm run dev` - start the development server
- `npm run build` - build for production
- `npm run preview` - preview the production build
- `npm run test` - run unit tests

## Configuration

Create a `.env` file based on `.env.example` to configure API endpoints.

## Legal Disclaimer

This application is provided for authorized security testing only. Always obtain permission before scanning any domains that you do not own or explicitly have permission to test.
